# 测试指南

## 预检查

### 1. 构建验证

```bash
pnpm build
```

应该无错误完成。

### 2. API 连接测试

```bash
pnpm test:api
```

这将测试：
- Polymarket API 可访问性
- 市场数据获取
- 价格检索

**预期输出：**
```
✅ API 测试成功完成！
```

**故障排除：**
- 网络错误：检查防火墙/VPN 设置
- SSL 错误：更新 Node.js 到最新 LTS
- API 变更：Polymarket 可能更新端点

## 手动测试

### 模拟模式（推荐）

1. 确保 `.env` 中有 `DRY_RUN=true`
2. 运行应用：

```bash
pnpm dev
```

3. 观察：
   - 配置显示
   - 市场监控消息
   - 机会检测（如果有）
   - 模拟交易执行

### 测试机会检测

降低利润阈值以更容易发现机会：

```bash
# 在 .env 中
MIN_PROFIT_MARGIN=0.001  # 0.1% 阈值
```

这将显示更多潜在交易用于测试。

### 测试交易流程

当机会出现时：

1. **查看详情**：检查 YES/NO 价格和预期利润
2. **确认交易**：提示时回答 `y`
3. **设置金额**：输入交易金额（例如 `10`）
4. **观察执行**：观看模拟交易完成
5. **检查日志**：查看 `logs/trades.log`

## 代码质量检查

### 类型检查

```bash
pnpm typecheck
```

应该无类型错误完成。

### 清洁构建

```bash
pnpm clean
pnpm build
```

验证项目从头开始构建。

## 日志验证

检查日志文件已创建：

```bash
ls -la logs/
```

应该看到：
- `combined.log` - 所有事件
- `error.log` - 仅错误
- `trades.log` - 交易活动

## 配置测试

在 `.env` 中测试不同配置：

### 高频监控

```env
POLL_INTERVAL_MS=5000  # 5 秒
```

### 保守策略

```env
MIN_PROFIT_MARGIN=0.05  # 5% 最小值
MIN_LIQUIDITY=5000      # 更高流动性要求
```

### 激进策略

```env
MIN_PROFIT_MARGIN=0.005  # 0.5% 最小值
MAX_TRADE_AMOUNT=50      # 较低最大金额
```

## 预期行为

### 正常运行

- 安静监控（最小控制台输出）
- 错误仅记录到文件
- 清晰的机会通知
- 流畅的 CLI 交互

### 错误场景

**API 不可用：**
- 启动时显示错误消息
- 记录在 `error.log` 中
- 优雅退出

**配置无效：**
- 清晰的错误消息
- 更正提示
- 在开始监控前退出

**交易期间中断：**
- Ctrl+C 时干净关闭
- 显示最终统计
- 取消待处理交易

## 性能基准

**典型指标：**
- 市场扫描：2-5 秒（100 个市场）
- API 延迟：每个请求 200-500ms
- 内存使用：~50-100MB
- CPU 使用：<5%（空闲），~20%（扫描）

## 已知限制

1. **实盘交易**：未完全实现，需要 CLOB 集成
2. **网络错误**：可能需要改进重试逻辑
3. **速率限制**：没有内置的速率限制处理
4. **WebSocket**：未实现，仅使用轮询

## 生产前步骤

在实盘交易前：

- [ ] 实现完整的 CLOB 订单执行
- [ ] 添加全面的错误恢复
- [ ] 实现速率限制处理
- [ ] 添加持仓跟踪
- [ ] 设置监控/警报
- [ ] 首先用小额测试
- [ ] 监控 Polygon 上的 Gas 成本
