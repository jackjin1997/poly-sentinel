# 快速开始指南

## 安装

```bash
# 使用 pnpm 安装依赖
pnpm install
```

## 配置

复制环境变量示例文件：

```bash
cp .env.example .env
```

默认配置已经可以在**模拟模式**下测试（不会执行真实交易）。

### 关键设置

- `DRY_RUN=true` - 模拟交易，不花费真实资金
- `MIN_PROFIT_MARGIN=0.02` - 只显示利润率超过 2% 的机会
- `POLL_INTERVAL_MS=10000` - 每 10 秒检查一次市场

## 运行

```bash
# 开发模式
pnpm dev

# 构建并运行
pnpm build
pnpm start
```

## 预期行为

1. **启动**：显示配置信息并连接到 Polymarket API
2. **监控**：每 10 秒扫描一次市场
3. **发现机会**：发现机会时显示详情并提示操作
4. **执行**：如果批准，则模拟交易（在模拟模式下）
5. **统计**：退出时显示摘要（按 Ctrl+C）

## 示例会话

```
═══════════════════════════════════════════════════════════════
   POLY SENTINEL - Polymarket 套利机器人
═══════════════════════════════════════════════════════════════
   模式：🧪 模拟运行
   最小利润：2.0%
   最大交易：$100
═══════════════════════════════════════════════════════════════

🔍 监控市场中...

────────────────────────────────────────────────────────────────

📊 比特币是否会在 2024 年底达到 $50k？
   YES: $0.4500
   NO:  $0.5200
   总计: $0.9700
   预期利润: $0.0300 (3.09%)
   流动性: $5000

? 执行此交易？(y/N) y
? 交易金额 (USD): 50

⚡ 正在执行交易：比特币是否会在 2024 年底达到 $50k？...

✅ 交易执行成功！利润：$1.4500

🔍 继续监控...
```

## 下一步

- 在模拟模式下监控以了解系统运作
- 根据需要调整 `MIN_PROFIT_MARGIN` 和 `POLL_INTERVAL_MS`
- 实盘交易：设置钱包凭证并将 `DRY_RUN=false`
- 查看 `logs/` 目录获取详细活动日志

## 故障排除

**找不到机会？**
- 降低 `MIN_PROFIT_MARGIN`（例如 0.01 代表 1%）
- 提高轮询频率：降低 `POLL_INTERVAL_MS`
- 套利机会稀少且竞争激烈

**API 错误？**
- 检查互联网连接
- 验证 Polymarket API 可访问性
- 查看 `logs/error.log`
